{
  "name": "fractions-kids",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Adaptive fractions game that helps kids (ages 7-9) build automaticity in fraction calculations.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
